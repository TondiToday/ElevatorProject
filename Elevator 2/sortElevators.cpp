
#include "sortElevators.h"
/*#include "Elevator.h"

#include <vector>


enum Direction { JOBS_DOWN = 0, JOBS_UP = 1, DIRECTION = 2, DISTANCE = 3 };


Elevator findBestElevator(vector<Elevator>& elevators, int& direction, int& request_at_floor)
{
	



}


bool compareJobsUp(Elevator* elevator1, Elevator* elevator2)
{
	return elevator1->ele_up.getsize() < elevator2->ele_up.getsize();
}


bool compareJobDown(Elevator* elevator1, Elevator* elevator2)
{
	return elevator1->ele_down.getsize() < elevator2->ele_down.getsize();
}

*/